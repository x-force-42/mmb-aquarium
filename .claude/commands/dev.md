---
description: Start the Vite dev server
allowed-tools: Bash
---

Start the Vite dev server in the background with `npm run dev` and report the URL it's listening on (default `http://localhost:5173`).

If the server fails to start, read the error output, fix the underlying issue (don't suppress it), and try again.
