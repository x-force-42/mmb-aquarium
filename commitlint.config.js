/**
 * Commit message policy: Conventional Commits.
 *
 * Scopes are intentionally **free-form** for now (see
 * `docs/guardrails-survey.md`, "Resolved decisions" #4). Once a stable
 * scope vocabulary emerges, this is where it gets enumerated.
 */
export default {
  extends: ['@commitlint/config-conventional'],
};
