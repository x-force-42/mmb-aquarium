node_modules
dist
coverage
test-results
playwright-report
.vite
*.log
.eslintcache

# Husky internals (regenerated on `npm install` via the prepare script).
.husky/_/

# Per-developer Vite env files. Only `.env.example` ships in git.
.env
.env.local

# Claude Code per-user settings (project-level `settings.json` would be committed)
.claude/settings.local.json

# Claude Code worktrees — per-session working copies, never part of the repo
.claude/worktrees/
