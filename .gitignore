node_modules
dist
coverage
test-results
playwright-report
.vite
*.log

# Claude Code per-user settings (project-level `settings.json` would be committed)
.claude/settings.local.json

# Claude Code worktrees — per-session working copies, never part of the repo
.claude/worktrees/
